version: 2
general:
  artifacts:
    - bolero-dev
  branches:
    ignore:
      - gh-pages
jobs:
  build:
    docker:
      - image: af01/bolero_dependencies:latest
    steps:
      - checkout
      - run:
          name: Build
          command: .circleci/build.sh
      - run:
          name: Test
          command: .circleci/test.sh
